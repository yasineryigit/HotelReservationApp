package com.ossovita.userservice.service;

import com.ossovita.userservice.entity.BusinessPosition;

public interface BusinessPositionService {
    BusinessPosition createBusinessPosition(BusinessPosition businessPosition);
}
